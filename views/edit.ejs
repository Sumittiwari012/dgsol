<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Profile</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap');

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(to right, #667eea, #764ba2);
      color: #fff;
      margin: 0;
      padding: 40px;
    }

    .container {
      max-width: 700px;
      background: rgba(255, 255, 255, 0.1);
      padding: 40px;
      margin: auto;
      border-radius: 15px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
    }

    h2 {
      text-align: center;
      margin-bottom: 30px;
    }

    label {
      display: block;
      margin-top: 20px;
      font-weight: 500;
    }

    input, select, textarea {
      width: 100%;
      padding: 10px;
      margin-top: 5px;
      border-radius: 8px;
      border: none;
      font-family: 'Poppins', sans-serif;
      font-size: 1rem;
    }

    textarea {
      resize: vertical;
      min-height: 80px;
    }

    .radio-group, .checkbox-group {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      margin-top: 10px;
    }

    .btn {
      margin-top: 30px;
      padding: 12px 25px;
      background-color: #4caf50;
      color: white;
      border: none;
      border-radius: 10px;
      font-weight: 600;
      cursor: pointer;
      width: 100%;
      font-size: 1rem;
    }

    .btn:hover {
      background-color: #43a047;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Edit Your Profile</h2>
    <form action="/edit" method="POST">
      <label for="fullName">Full Name</label>
      <input type="text" id="fullName" name="fullName" value="<%= user.fullName %>" required />

      <label for="dob">Date of Birth</label>
      <input type="date" id="dob" name="dob" value="<%= user.dob %>" required />

      <label>Gender</label>
      <div class="radio-group">
        <label><input type="radio" name="gender" value="Male" <% if (user.gender === 'Male') { %>checked<% } %>> Male</label>
        <label><input type="radio" name="gender" value="Female" <% if (user.gender === 'Female') { %>checked<% } %>> Female</label>
        <label><input type="radio" name="gender" value="Other" <% if (user.gender === 'Other') { %>checked<% } %>> Other</label>
      </div>

      <label for="phone">Phone Number</label>
      <input type="tel" id="phone" name="phone" value="<%= user.phone %>" required />

      <label for="address">Address</label>
      <input type="text" id="address" name="address" value="<%= user.address %>" required />
      <label for="country">Country</label>
      <input type="text" id="country" name="country" value="<%= user.country %>" />

      <label for="bio">About You</label>
      <textarea id="bio" name="bio"> <%= user.bio %></textarea>

      <label>Health Preferences</label>
      <div class="checkbox-group">
        <label><input type="checkbox" name="healthPreferences" value="Anxiety" <%= user.healthPreferences?.includes('Anxiety') ? 'checked' : '' %>> Anxiety</label>
        <label><input type="checkbox" name="healthPreferences" value="Depression" <%= user.healthPreferences?.includes('Depression') ? 'checked' : '' %>> Depression</label>
        <label><input type="checkbox" name="healthPreferences" value="Stress" <%= user.healthPreferences?.includes('Stress') ? 'checked' : '' %>> Stress</label>
        <label><input type="checkbox" name="healthPreferences" value="Burnout" <%= user.healthPreferences?.includes('Burnout') ? 'checked' : '' %>> Burnout</label>
        <label><input type="checkbox" name="healthPreferences" value="None" <%= user.healthPreferences?.includes('None') ? 'checked' : '' %>> None of the above</label>
      </div>

      <label>Preferred Support Type</label>
      <div class="checkbox-group">
        <label><input type="checkbox" name="preferredSupport" value="Therapy" <%= user.preferredSupport?.includes('Therapy') ? 'checked' : '' %>> Therapy</label>
        <label><input type="checkbox" name="preferredSupport" value="Community" <%= user.preferredSupport?.includes('Community') ? 'checked' : '' %>> Community Support</label>
        <label><input type="checkbox" name="preferredSupport" value="Self-care" <%= user.preferredSupport?.includes('Self-care') ? 'checked' : '' %>> Self-Care Resources</label>
        <label><input type="checkbox" name="preferredSupport" value="Workshops" <%= user.preferredSupport?.includes('Workshops') ? 'checked' : '' %>> Workshops/Webinars</label>
        <label><input type="checkbox" name="preferredSupport" value="None" <%= user.preferredSupport?.includes('None') ? 'checked' : '' %>> None of the above</label>
      </div>

      <button class="btn" type="submit">Save Changes</button>
    </form>
  </div>
</body>
</html>
