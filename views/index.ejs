<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Welcome to Mental Health App</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: linear-gradient(45deg, #ff6b6b, #f7ff6b);
      color: #fff;
      text-align: center;
    }

    .content {
      padding: 80px 20px 40px;
      max-width: 800px;
      margin: 0 auto;
      filter: brightness(1.2);
    }

    /* Responsive Navbar */
    .navbar {
      width: 90%;
      background-color: rgba(51, 51, 51, 0.7);
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: center;
      gap: 15px;
      padding: 10px 15px;
      box-sizing: border-box;
      margin: 0 auto;
      border-radius: 10px;
    }

    .nav-button {
      background-color: #fff;
      color: #333;
      border: none;
      padding: 10px 16px;
      border-radius: 6px;
      cursor: pointer;
      font-weight: bold;
      transition: all 0.3s ease;
      text-decoration: none;
      white-space: nowrap;
      flex-shrink: 0;
    }

    .nav-button:hover {
      background-color: #ff6b6b;
      color: #fff;
    }

    /* Login & Signup Buttons Top Right */
    .auth-buttons {
      position: fixed;
      top: 15px;
      right: 20px;
      display: flex;
      gap: 10px;
      z-index: 1000;
    }

    .auth-buttons button {
      background-color: #fff;
      color: #333;
      border: none;
      padding: 8px 14px;
      border-radius: 5px;
      font-weight: 600;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    .auth-buttons button:hover {
      background-color: #ff6b6b;
      color: #fff;
    }

    @media (max-width: 600px) {
      .navbar {
        flex-direction: column;
      }
      .auth-buttons {
        flex-direction: column;
        top: 10px;
        right: 10px;
      }
      .nav-button {
        width: 100%;
        max-width: 300px;
      }
    }
  </style>
</head>
<body>

  <!-- Login and Sign Up -->
  <div class="auth-buttons">
    <button onclick="window.location.href='login'">Login</button>
    <button onclick="window.location.href='signup'">Sign Up</button>
  </div>

  <!-- Welcome Section -->
  <div class="content">
    <h1>Welcome to Mental Health App</h1>
    <p>Your companion for better mental health and well-being.</p>
  </div>

  <!-- Responsive Navbar with Quick Access -->
  <nav class="navbar">
    <a class="nav-button" href="/therapy">Therapy Directory</a>
    <a class="nav-button" href="/self_care">Self-Care Resources</a>
    <a class="nav-button" href="community/forum.html">Community Forum</a>
    <a class="nav-button" href="/corporate">Corporate Dashboard</a>
    <a class="nav-button" href="checkup">Quick checkup</a>
  </nav>

</body>
</html>
